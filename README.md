# Peer-Review
I’ve removed all unnecessary files to keep things simple. That said, you won’t be able to run the tests on the code. 
All questions passed the tests, so the code functions correctly when reviewed individually.
